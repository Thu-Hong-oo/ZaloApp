package com.zalo.auth.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // CORS configuration removed as it's handled by the API Gateway
} 